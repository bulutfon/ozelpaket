!!!
%html
  %head
    %meta{charset: 'utf-8'}/
    %meta{content: 'IE=edge,chrome=1', 'http-equiv' => 'X-UA-Compatible'}/
    %meta{content: 'width=device-width, initial-scale=1', name: 'viewport'}/

    = yield_content :meta

    %title= current_page.data.title || 'Özel Paket'

    = stylesheet_link_tag 'application',
                      '//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css',
                      '//fonts.googleapis.com/css?family=Roboto:400,100,300,300italic,500,700,900'

    = yield_content :head

  %body{class: page_classes}

    = yield

    = javascript_include_tag  'application',
                               '//ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js'
    :javascript
      //Fallback to loading jQuery from a local path if the CDN is unavailable
      window.jQuery || document.write('<script src="assets/javascripts/vendor/jquery-1.11.3.min.js"><\/script>')

    = yield_content :foot